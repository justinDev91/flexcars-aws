# name: Deploy Backend to EKS

# on:
#   push:
#     branches: [main]
#     paths:
#       - 'src/flexcars-backend/**'
#       - 'k8s/flexcars-backend.yaml'
#       - 'k8s/ingress.yaml'

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: eu-west-3

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build, tag, and push image to ECR
#         run: |
#           docker build -t ${{ secrets.ECR_BACKEND_REPO }}:${{ github.sha }} ./src/flexcars-backend
#           docker push ${{ secrets.ECR_BACKEND_REPO }}:${{ github.sha }}

#       - name: Update kubeconfig
#         run: |
#           aws eks update-kubeconfig --region eu-west-3 --name ${{ secrets.EKS_CLUSTER_NAME }}

#       - name: Set image tag in manifest
#         run: |
#           sed -i 's#<ECR_BACKEND_IMAGE_URL>:latest#${{ secrets.ECR_BACKEND_REPO }}:${{ github.sha }}#g' k8s/flexcars-backend.yaml

#       - name: Deploy to EKS
#         run: |
#           kubectl apply -f k8s/flexcars-backend.yaml
#           kubectl apply -f k8s/ingress.yaml
